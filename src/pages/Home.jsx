import { Hero, Slider } from '../components';

const Home = () => {
  return (
    <section className='relative'>
      {/* -- Hero -- */}
      <Hero />
      {/* Slider */}
      <Slider />
    </section>
  );
};

export default Home;
