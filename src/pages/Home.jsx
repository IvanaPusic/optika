import { Hero, Slider } from '../components';

const Home = () => {
  return (
    <section>
      {/* -- Hero -- */}
      <Hero />
      {/* Slider */}
      <Slider />
    </section>
  );
};

export default Home;
